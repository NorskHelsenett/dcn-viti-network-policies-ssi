services:
  viti-network-policies-ssi:
    build: .
    container_name: viti-network-policies-ssi
    image: viti-network-policies-ssi
    user: "1993:1993"
    environment:
      - SECRETS_PATH=/app/secrets/secrets.yaml
      - CONFIG_PATH=/app/config/config.yaml
    volumes:
      - ./config/config.yaml:/app/config/config.yaml:ro
      - ./secrets/secrets.yaml:/app/secrets/secrets.yaml:ro
