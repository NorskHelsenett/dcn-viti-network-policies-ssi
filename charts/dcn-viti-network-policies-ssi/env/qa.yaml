schedule: "*/15 * * * *"

limits:
  memory:
    min: "384Mi"
    max: "1152Mi"
  cpu:
    min: "300m"
    max: "600m"

settings:
  environment: production
  infrastructure: qa
  interval: 300
  continuousMode: false

integration:
  nam:
    url: "https://qa.dcn.nhn.no:3000/api"
  splunk:
    url: "https://splunk-hec.nhn.no"
    index: "dc_nam"
    source: "dcn-viti-network-policies-ssi:qa"

credentials:
  namToken: "{{ .Values.credentials.namToken }}"
  splunkToken: "{{ .Values.credentials.splunkToken }}"
workspace: ssi
